import React from "react";
import { Element } from "react-scroll";

const Features = () => {
  return (
    <section>
      <Element name="features" />
    </section>
  );
};

export default Features;
